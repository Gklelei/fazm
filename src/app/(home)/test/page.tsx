import BatchWithSessionsForm from "@/Modules/Settings/Batches/Ui/CreateBatch";

const page = async () => {
  return (
    <div>
      <BatchWithSessionsForm />
    </div>
  );
};

export default page;
